<h1>Add Appointment</h1>

<%= form_with(model: [@doctor, @appt], local: true) do |f| %>

<%= f.label :date%>
<%= f.date_field :date, required: true%>
<br />

<%= f.label :date%>
<%= f.date_field :date, required: true%>
<br />


<%= f.label :user %>
<%= f.select :user_id, User.all.collect { |u|% [u.first_name, u.id]}%>
<br />


<%= f.submit nil %>

<% end%>

