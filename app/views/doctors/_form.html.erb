<h1> <%= @doctor.id ? "Edit #{@doctor.name}" : "New Doctor" %></h1>

<%= form_for @doctor do |f| %>

<%= f.label :name %>
<% f.text_field :name %>

<%= f.label :profession%>
<% f.text_field :profession%>

<%= f.label :md%>
<% f.check_box :md%>

<% f.submit %>

<% end%>

